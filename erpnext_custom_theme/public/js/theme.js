// ERPNext Custom Theme
// Minimal JS file for Frappe v16 compatibility
// Theme CSS is loaded via website_context in hooks.py
frappe.require = frappe.require || function() { return Promise.resolve(); };
